{
  "name": "kara-substack-feed-api",
  "version": "0.1.0",
  "bin": {
    "kara-substack-feed-api": "bin/kara-substack-feed-api.js"
  },
  "scripts": {
    "build": "tsc",
    "build:lambda": "tsc --project lambda/tsconfig.json",
    "build:all": "npm run build && npm run build:lambda",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "22.7.9",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "~5.6.3"
  },
  "dependencies": {
    "aws-cdk-lib": "2.181.1",
    "aws-sdk": "^2.1692.0",
    "constructs": "^10.0.0"
  }
}
